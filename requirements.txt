# ============================================================================
# STG-Mol — Python dependencies
# ============================================================================
# Recommended install order (see README.md for CUDA-specific instructions):
#   1) PyTorch and PyTorch Geometric (with the correct CUDA build)
#   2) `pip install -r requirements.txt`
# ============================================================================

# ---- Deep learning ----
# PyTorch is installed separately (see README) so we only pin a minimum here.
torch>=1.12,<2.5
torch-geometric>=2.1,<2.6
# Extra PyG operators (install via -f https://data.pyg.org/whl/…):
#   pyg-lib torch-scatter torch-sparse torch-cluster

# ---- Cheminformatics ----
rdkit>=2022.09.1
mol2vec>=0.1                   # 1D pre-trained fragment embeddings
gensim>=4.0                    # required by mol2vec

# ---- Scientific stack ----
numpy>=1.21,<2.0
pandas>=1.3
scikit-learn>=1.0
scipy>=1.7

# ---- Traditional ML baselines (for the QSAR benchmark) ----
xgboost>=1.6
lightgbm>=3.3

# ---- Configuration & utilities ----
pyyaml>=6.0
tqdm>=4.62
joblib>=1.1

# ---- Visualisation ----
matplotlib>=3.4
seaborn>=0.11

# ---- Optional: chemical-space visualisation ----
umap-learn>=0.5

# ---- ADMET & structural analysis (used by the validation pipeline) ----
biopython>=1.79
openpyxl>=3.0

# ---- Development / testing (optional) ----
# jupyter
# pytest
# black
